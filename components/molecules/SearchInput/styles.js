import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  TextInput: {
    paddingLeft: 15,
    borderRadius: 10,
    borderWidth: 3,
    color: '#757575',
    borderColor: '#ECECEC',
    backgroundColor: 'white',
    margin: 10,
  },
});
