import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  tagLabel: {
    fontWeight: 'bold',
    color: '#757575',
  },
  tagContainer: {
    borderWidth: 1,
    padding: 4,
    margin: 10,
    borderRadius: 20,
    display: 'flex',
    flexDirection: 'row',
  },
});
